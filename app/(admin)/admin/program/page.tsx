import Image from 'next/image'
import Link from 'next/link'
import { CheckCircle2, Edit2, ImageIcon, Trash2 } from 'lucide-react'
import { getPrograms } from '@/lib/academics'
import { deleteProgram } from './actions'
export default async function AdminProgram({searchParams}:{searchParams:Promise<{saved?:string}>}) { const [programs,q]=await Promise.all([getPrograms(),searchParams]); return <div className="space-y-6">{q.saved==='1'&&<div className="flex items-center gap-2 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-800"><CheckCircle2 size={18}/>Data program berhasil disimpan.</div>}<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4 text-center">No.</th><th className="px-3 py-4">Preview</th><th className="px-4 py-4">Program</th><th className="px-4 py-4">Usia & Jadwal</th><th className="px-4 py-4">Status</th><th className="px-5 py-4 text-right">Aksi</th></tr></thead><tbody className="divide-y divide-slate-100">{programs.map((p,i)=><tr key={p.id} className="hover:bg-slate-50"><td className="px-5 py-4 text-center text-xs text-slate-400">{i+1}</td><td className="px-3 py-3"><div className="relative h-14 w-20 overflow-hidden rounded-lg bg-slate-100">{p.image?<Image src={p.image} alt={p.title} fill sizes="80px" className="object-cover"/>:<span className="flex h-full items-center justify-center text-slate-300"><ImageIcon size={22}/></span>}</div></td><td className="px-4 py-4"><strong className="block text-slate-900">{p.title}</strong><span className="text-xs text-slate-500">{p.eyebrow}</span>{p.isFeatured&&<span className="ml-2 rounded-full bg-amber-100 px-2 py-0.5 text-[10px] font-bold text-amber-700">Unggulan</span>}</td><td className="px-4 py-4"><span className="block">{p.age}</span><span className="text-xs text-slate-500">{p.schedule}</span></td><td className="px-4 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${p.isActive?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{p.isActive?'Aktif':'Nonaktif'}</span></td><td className="px-5 py-4"><div className="flex justify-end gap-1"><Link href={`/admin/program/edit/${p.id}`} className="p-2 text-slate-400 hover:text-blue-600" title="Edit"><Edit2 size={17}/></Link><form action={deleteProgram.bind(null,p.id)}><button className="p-2 text-slate-400 hover:text-red-600" title="Hapus"><Trash2 size={17}/></button></form></div></td></tr>)}</tbody></table></div>{!programs.length&&<div className="p-16 text-center text-sm text-slate-500">Belum ada program.</div>}<div className="border-t border-slate-200 p-4 text-sm text-slate-500">{programs.length} program</div></div></div> }
