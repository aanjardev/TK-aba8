import {CheckCircle2,Save} from 'lucide-react';import {getSchoolContact} from '@/lib/school-contact';import{updateContact}from'./actions';import TabbedFormSections from '@/components/admin/TabbedFormSections';import{SubmitButton}from'@/components/admin/FormControls';
const field='w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm';
export default async function Page({searchParams}:{searchParams:Promise<{saved?:string}>}){const[data,q]=await Promise.all([getSchoolContact(),searchParams]);return <div className="space-y-6">{q.saved==='1'&&<div className="rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-800"><CheckCircle2 className="mr-2 inline" size={18}/>Informasi kontak berhasil disimpan.</div>}<form action={updateContact}><TabbedFormSections actions={<SubmitButton className="inline-flex items-center gap-2 rounded-xl bg-amber-500 px-5 py-2.5 text-sm font-bold text-emerald-950"><Save size={17}/>Simpan Perubahan</SubmitButton>} tabs={[
{id:'tampilan',label:'Tampilan',description:'Judul dan deskripsi halaman kontak.',content:<><Field label="Judul"><input name="sectionTitle" defaultValue={data.sectionTitle} className={field} required/></Field><Field label="Deskripsi"><textarea name="sectionDescription" defaultValue={data.sectionDescription} rows={3} className={field} required/></Field></>},
{id:'kontak',label:'Alamat & Kontak',description:'Informasi alamat dan komunikasi sekolah.',content:<><Field label="Alamat"><textarea name="address" defaultValue={data.address} rows={3} className={field} required/></Field><div className="grid gap-4 md:grid-cols-3">{[['district','Kecamatan',data.district],['regency','Kabupaten',data.regency],['postalCode','Kode pos',data.postalCode],['phone','Telepon',data.phone],['whatsapp','WhatsApp (format 62)',data.whatsapp],['email','Email',data.email]].map(([name,label,value])=><Field key={name} label={label}><input name={name} type={name==='email'?'email':undefined} defaultValue={value} className={field}/></Field>)}</div><Field label="Waktu pelayanan"><input name="serviceHours" defaultValue={data.serviceHours} className={field}/></Field></>},
{id:'peta',label:'Peta & WhatsApp',description:'Peta lokasi dan pesan WhatsApp.',content:<><Field label="URL embed Google Maps"><textarea name="mapEmbedUrl" defaultValue={data.mapEmbedUrl} rows={3} className={field}/></Field><Field label="URL petunjuk arah"><input name="mapDirectionsUrl" defaultValue={data.mapDirectionsUrl} className={field}/></Field><Field label="Pembuka pesan WhatsApp"><textarea name="whatsappDefaultMessage" defaultValue={data.whatsappDefaultMessage} rows={3} className={field}/></Field></>}
]}/></form></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="mb-2 block text-sm font-semibold">{label}</span>{children}</label>}
