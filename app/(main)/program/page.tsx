import type {Metadata} from 'next';import Image from 'next/image';import Link from 'next/link';import{ArrowRight,Clock,ImageIcon,Users}from'lucide-react';import{getPrograms}from'@/lib/academics';import{getSiteSettings}from'@/lib/site-settings';
export async function generateMetadata():Promise<Metadata>{const site=await getSiteSettings();return{title:'Program Pendidikan',description:`Daftar program pendidikan dan pengembangan minat anak di ${site.shortName}.`}}
export default async function ProgramsPage(){const programs=await getPrograms({activeOnly:true});return <main className="bg-white"><section className="border-b border-emerald-100 bg-[#f4fcf7] px-6 py-20 text-center"><div className="container mx-auto max-w-3xl"><span className="text-xs font-extrabold uppercase tracking-[.2em] text-[#00923f]">Pilihan pendidikan</span><h1 className="mt-4 text-4xl font-extrabold text-[#0a1c12] md:text-5xl">Program untuk Setiap Tahap Tumbuh</h1><p className="mt-5 leading-relaxed text-[#6b8077]">Program dirancang sesuai usia, kesiapan, dan kebutuhan perkembangan anak dalam lingkungan Islami yang menyenangkan.</p></div></section><section className="px-6 py-20"><div className="container mx-auto grid max-w-6xl gap-6 md:grid-cols-2">{programs.map(p=><article key={p.id} className="group overflow-hidden rounded-3xl border border-slate-100 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="relative aspect-[16/9] bg-emerald-50">{p.image?<Image src={p.image} alt={p.title} fill sizes="(max-width:768px) 100vw,50vw" className="object-cover transition duration-500 group-hover:scale-105"/>:<span className="flex h-full items-center justify-center text-emerald-200"><ImageIcon size={44}/></span>}</div><div className="p-6"><span className="text-xs font-bold uppercase tracking-wider text-[#00923f]">{p.eyebrow}</span><h2 className="mt-2 text-xl font-extrabold text-[#0a1c12]">{p.title}</h2><p className="mt-3 line-clamp-3 text-sm leading-relaxed text-[#6b8077]">{p.description}</p><div className="mt-5 flex flex-wrap gap-4 border-t border-slate-100 pt-4 text-xs font-semibold text-slate-500"><span className="flex items-center gap-1.5"><Users size={15}/>{p.age}</span><span className="flex items-center gap-1.5"><Clock size={15}/>{p.schedule}</span></div><Link href={`/program/${p.slug}`} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-[#00923f]">Lihat program<ArrowRight size={16}/></Link></div></article>)}</div></section></main>}
